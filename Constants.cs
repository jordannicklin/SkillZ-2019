using ElfKingdom;

namespace SkillZ {
    public static class Constants {
        /// <summary>
        /// Warning! Disable this during production/serious bot match runnings!
        /// </summary>
        public const bool EnableDebug = true;

        public static Game Game;

        public static GameCaching GameCaching;
    }
}
